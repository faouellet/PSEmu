version: 0.0.{build}

pull_requests:
  do_not_increment_build_number: true

branches:
  only:
  - master
  - travis

image: Visual Studio 2017

build:
  verbosity: minimal

build_script:
- cmd: >-
    mkdir cmake-build
    cd cmake-build
    cmake --build . --config Release